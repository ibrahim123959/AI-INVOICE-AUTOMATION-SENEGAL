cat > ml-engine/.env.example << 'EOF'
# Model Configuration
MODEL_NAME=paraphrase-multilingual-MiniLM-L12-v2
CACHE_DIR=/app/models
DEVICE=cpu  # cpu or cuda

# Cache Settings
ENABLE_CACHE=true
CACHE_SIZE=500  # Number of accounts to cache

# Logging
LOG_LEVEL=INFO
EOF